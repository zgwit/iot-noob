#pragma once

class Expression
{
private:
    /* data */
public:
    Expression(/* args */);
    ~Expression();

    bool Parse(const char* expr);

    
};

Expression::Expression(/* args */)
{
}

Expression::~Expression()
{
}
